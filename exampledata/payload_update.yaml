payload_id: 333
inputs:
  - filename: Muncie.p04.tmp.hdf
    resource_info:
      store: s3
      root: cloud-wat-dev
      path: /data/Muncie/Muncie.p04.tmp.hdf
    internal_paths: ## there might be dozens, each a different type.
      - pathname: '/Event Conditions/15696.24'
        resource_info:
          store: s3
          root: cloud-wat-dev
          path: /runs/event_1/hsm/hsm1.csv
      - pathname: '/Event Conditions/789563.89'
        resource_info:
          store: s3
          root: cloud-wat-dev
          path: /runs/event_1/hsm/hsm2.csv
  - filename: Muncie.b04
    resource_info:
      store: s3
      root: cloud-wat-dev
      path: /data/Muncie/Muncie.p04.tmp.hdf
    internal_paths:
      - pathname: 'BreachLocationA' #not sure how these are actually stored
        resource_info:
          store: s3
          root: cloud-wat-dev
          path: /runs/event_1/fc/fc1.csv
      - pathname: 'Boundary4' # not sure how these are actually stored.
        filename: hsm3.dss
        internal_path: /a/b/c/d/e/f
        resource_info:
          store: s3
          root: cloud-wat-dev
          path: /runs/event_1/hsm/hsm3.dss
outputs:
  - data:
      id: 1
      name: Muncie.p04.tmp.hdf
  - data:
      id: 2
      name: Muncie.b04