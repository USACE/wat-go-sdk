manifest_id: int
requirements:
  - plugin:
     name: string
     image_and_tag: string
     command: []string
  - model_identifier:
     name: string
     alternative: string
  - inputs:
     types: 
        - file: ##do we really anticipate additional types?
           name: string
           path: string, optional
  - outputs:
     types: 
        - file:
           name: string
           path: string, optional